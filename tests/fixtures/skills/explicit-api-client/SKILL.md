---
name: explicit-api-client
description: Query a private service; requires an API key before this skill can run.
---

# Explicit API Client

## Requirements

Set `EXAMPLE_API_KEY` before calling the service.
